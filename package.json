{
  "name": "tsno",
  "version": "0.0.0",
  "description": "My awesome typescript library",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "/cli.js"
  ],
  "main": "./dist/index.js",
  "bin": "./cli.js",
  "scripts": {
    "build": "tsup src/index.ts && tsup src/source-map-support-inject.js --minify",
    "test": "npm run build && jest",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@egoist/prettier-config": "^0.1.0",
    "@sucrase/jest-plugin": "^2.0.0",
    "@types/cross-spawn": "^6.0.2",
    "@types/jest": "^26.0.20",
    "axios": "^0.22.0",
    "execa": "^5.1.1",
    "jest": "^26.6.3",
    "minimost": "^1.2.0",
    "picocolors": "^1.0.0",
    "prettier": "^2.2.1",
    "source-map-support": "^0.5.20",
    "tsup": "^5.4.0",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "bundle-require": "^1.3.0",
    "cross-spawn": "^7.0.3",
    "esbuild": "^0.13.4"
  }
}
